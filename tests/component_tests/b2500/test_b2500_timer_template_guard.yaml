external_components:
  - source: COMPONENT_PATH
    refresh: 0s

wifi:
  ssid: "test"
  password: "12345678"

esp32:
  board: esp32dev

ble_client:
  mac_address: "00:00:00:00:00:00"

b2500:
  id: battery
  generation: 2

logger:

esphome:
  name: test

time:
  - platform: sntp
    id: sntp_time

interval:
  - interval: 60s
    then:
      - b2500.set_timer:
          id: battery
          generation: 2
          timer: !lambda 'return 99;'
          enabled: true
